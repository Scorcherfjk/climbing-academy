---
import { Icon } from "astro-icon/components";
import type { Review } from "../interfaces";

const reviews: Review[] = [
  {
    title: "¡Engancha desde el primer día!",
    review:
      "Vine a probar una clase guiada y terminé sacando un plan mensual. Los instructores son súper atentos y el ambiente es muy motivador.",
    name: "Lucía P.",
    stars: 5,
  },
  {
    title: "Ideal para escalar a tu ritmo",
    review:
      "Me gusta venir en mis horarios libres y escalar sin presión. El espacio está bien cuidado y el sistema de alquiler de equipo es muy práctico.",
    name: "Diego R.",
    stars: 4,
  },
  {
    title: "Gran lugar para empezar",
    review:
      "Nunca había escalado antes y me sentí muy cómodo desde el primer momento. Me explicaron todo y me ayudaron a perder el miedo.",
    name: "Andrea M.",
    stars: 5,
  },
];
---

<section class="bg-gray-50">
  <div class="mx-auto max-w-screen-2xl px-4 py-12 sm:px-6 lg:px-8 lg:py-16">
    <div class="md:flex md:items-end md:justify-between">
      <div class="max-w-2xl">
        <h2 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
          Lo que dicen quienes ya escalaron con nosotros
        </h2>

        <p class="mt-6 max-w-2xl leading-relaxed text-gray-700">
          Descubre por qué cada vez más personas eligen nuestro muro para
          entrenar, aprender y divertirse. Estas son algunas de las experiencias
          reales de quienes ya forman parte de la comunidad.
        </p>
      </div>
    </div>

    <div class="mt-8 grid grid-cols-1 gap-4 md:grid-cols-3">
      {
        reviews.map((review) => (
          <blockquote class="flex h-full flex-col justify-between bg-white p-6 shadow-xs sm:p-8">
            <div>
              <div class="flex gap-0.5 text-yellow-500">
                {Array.from({ length: 5 }).map((_, index) => (
                  <Icon
                    name={index < review.stars ? "star-full" : "star-empty"}
                    class="size-5"
                  />
                ))}
              </div>

              <div class="mt-4">
                <p class="text-2xl font-bold text-blue-950 sm:text-3xl">
                  {review.title}
                </p>

                <p class="mt-4 leading-relaxed text-gray-700">
                  {review.review}
                </p>
              </div>
            </div>

            <footer class="mt-4 text-sm font-medium text-gray-700 sm:mt-6">
              &mdash; {review.name}
            </footer>
          </blockquote>
        ))
      }
    </div>
  </div>
</section>
